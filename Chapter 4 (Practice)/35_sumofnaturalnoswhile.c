#include <stdio.h> 

int main()
{
    int i, sum=0;
    while (i<=98)
    {
        sum +=i;
        i++;
    }
        printf("the sum of first 98 natural numbers is : %d ", sum);
    
    return 0;
}