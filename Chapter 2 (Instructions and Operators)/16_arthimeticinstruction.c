#include <stdio.h>

int main()
{
    float a = 69;
    float b = 42;
    printf("the value of a + b is %f \n", a + b);
    printf("the value of a - b is %f \n", a - b);
    printf("the value of a * b is %f \n", a * b);
    printf("the value of a / b is %f \n", a / b);
    return 0;
}


//There is no operator to perform exponentiation in C
//To perform exponential : pow(x.y)= x^y